# BerlinDuck dependencies. Requires Python 3.11+.
#
# Recommended (works around old system Python, no root needed):
#   conda create -n berlinduck python=3.11
#   conda activate berlinduck
#   pip install -r requirements.txt
#
# Or with a plain venv on a 3.11+ interpreter:
#   python3.11 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# The package is not installed; run it with src/ on the path:
#   PYTHONPATH=src python -m berlinduck.demo "..."
# (pytest finds src/ automatically via conftest.py)

# --- runtime ---
numpy>=1.26,<3
pandas>=2.1,<3
datasets>=2.19,<4
sentence-transformers>=2.7,<4
qdrant-client>=1.12,<2

# --- dev ---
pytest>=8.2,<9
ruff>=0.5,<1
